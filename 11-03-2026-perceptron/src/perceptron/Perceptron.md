# Perceptron

Category: python

## Based on principles of perceptron

Just like the weights, the threshold is a real number which is a parameter of the neuron. To put it in more precise algebraic terms:

```
output = {
  0 if Σ_j w_j x_j <= threshold
  1 if Σ_j w_j x_j > threshold
}
```

That's all there is to how a perceptron works.

Create code that calculates your own example of a perceptron.
